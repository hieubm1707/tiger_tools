export type {{name.pascalCase()}} = {
    // insert formatted attributes here
};

export type Create{{name.pascalCase()}}DTO = {
    // insert create attributes here
};

export type Update{{name.pascalCase()}}DTO = {
    // insert update attributes here
};

export type Filter{{name.pascalCase()}}DTO = {
    // insert filter attributes here
};
